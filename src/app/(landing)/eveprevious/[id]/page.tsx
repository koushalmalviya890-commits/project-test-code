// import EventDetailPage from '@/components/events/eventdetailspage';

// // This page component will render your detailed event view.
// // It receives the event ID from the URL, which can be used later to fetch dynamic data.
// export default function Page({ params }: { params: { id: string } }) {
//   // For now, we are just rendering the static component.
//   // Later, you can pass params.id to EventDetailPage to fetch specific event data.
//   return <EventDetailPage />;
// }
